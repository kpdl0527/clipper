# the updater executable will sit next to yt_clipper

# get version of installed yt_clipper version on users local machine

def get_installed_version() : 
    version_number = ""
    return

def get_latest_version() : 
    version_number = ""
    return

def update(installed_version, lastest_version) :
    if (lastest_version > installed_version) :
        # update
        return